# Domains the link check skips because they always block automated requests.
# Records on these domains are re-checked by hand on their own review cycle instead.
# Add a domain only after it has come back "unknown" on repeated runs; say why and when.
#
# - domain: example.gov
#   reason: Always returns a bot challenge to automated requests
#   added: 2026-09-23
domains: []
